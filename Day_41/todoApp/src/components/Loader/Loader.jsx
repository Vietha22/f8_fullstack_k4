import React from "react";
import "./Loader.css"; // Đảm bảo bạn đã tạo một file CSS tương ứng để định dạng

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
